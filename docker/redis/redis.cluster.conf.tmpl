# 基础网络与安全
port 6379
protected-mode no
requirepass 123456
masterauth 123456

# 核心持久化策略（AOF + RDB 双保险）
appendonly yes
appendfsync everysec

# 开启集群模式核心配置
cluster-enabled yes
# 集群节点配置文件（由 Redis 内部自动维护，绝对不能手动修改）
cluster-config-file nodes.conf
# 节点失联超时时间（毫秒），超过此时间未响应则触发主从切换
cluster-node-timeout 5000